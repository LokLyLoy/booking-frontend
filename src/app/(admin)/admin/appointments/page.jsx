import React from 'react';
import Appointments from "@/components/(admin)/Appointments/Appointments";

const Page = () => {
    return (
        <div>
            <Appointments />
        </div>
    );
};

export default Page;